name: Publish to npm

on:
  push:
    branches:
      - main
    paths:
      - 'package.json'

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          registry-url: 'https://registry.npmjs.org/'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run build
        run: npm run build

      - name: Run core tests
        run: npm run test:core

      - name: Run lint
        run: npm run lint

      - name: Run type-check
        run: npm run type-check

      - name: Verify build output
        run: |
          test -f dist/index.js && echo "✓ CJS build exists"
          test -f dist/index.mjs && echo "✓ ESM build exists"
          test -f dist/index.d.ts && echo "✓ TypeScript definitions exist"
          test -f dist/linter/eslint-plugin/index.js && echo "✓ ESLint plugin CJS exists"
          test -f dist/linter/eslint-plugin/index.mjs && echo "✓ ESLint plugin ESM exists"
          test -f dist/linter/eslint-plugin/index.d.ts && echo "✓ ESLint plugin types exist"

      - name: Check version and publish
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "🔍 Checking version: $CURRENT_VERSION"
          
          # Check if version exists on npm
          if npm view test-a11y-js@$CURRENT_VERSION version > /dev/null 2>&1; then
            echo "⏭️  Version $CURRENT_VERSION already exists - skipping publish"
            echo "✅ No action needed"
          else
            echo "🚀 Publishing new version $CURRENT_VERSION..."
            npm publish --access public
            echo "✅ Successfully published test-a11y-js@$CURRENT_VERSION"
            echo "📦 Package: https://www.npmjs.com/package/test-a11y-js"
            echo "🎉 test-a11y-js is now available for installation!"
            echo ""
            echo "💡 To create a git tag manually:"
            echo "   git tag v$CURRENT_VERSION"
            echo "   git push origin v$CURRENT_VERSION"
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

